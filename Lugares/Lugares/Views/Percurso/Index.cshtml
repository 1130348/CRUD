@model IEnumerable<ClassLibrary.Model.Percurso>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PercursoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PercursoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PercursoID })
        </td>
    </tr>
}

</table>
